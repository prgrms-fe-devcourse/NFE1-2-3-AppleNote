export type UserSchemaType = {
  name: string;
  password: string;
  address: string;
  description: string;
  email: string;
  isCompleted?: boolean;
  bannerImage?: string;
  profileImage?: string;
};
