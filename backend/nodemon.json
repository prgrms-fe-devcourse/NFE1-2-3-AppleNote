{
  "watch": ["src"],
  "ext": "ts,js,json",
  "ignore": ["node_modules", "src/**/*.{spec,test}.ts", "coverage", "dist"],
  "exec": "ts-node -r tsconfig-paths/register ./src/bin/www.ts",
  "env": {
    "NODE_ENV": "dev"
  }
}
